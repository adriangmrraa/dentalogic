# ==================== DENTAL.ENV.EXAMPLE ====================
# Archivo de configuración para Clínica Dental (FASE 2)
# Copiar a .env y reemplazar los valores placeholders

# ==================== OPENAI & CORE ====================
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxx
INTERNAL_API_TOKEN=your-internal-secret-token-here

# ==================== DATABASE ====================
POSTGRES_DSN=postgresql://user:password@localhost:5432/clinica_dental_db
REDIS_URL=redis://redis:6379

# ==================== CLINIC INFORMATION (FASE 2) ====================
CLINIC_NAME=Clínica Dental Mercedes
CLINIC_ADDRESS=Calle Principal 123, Mercedes, Buenos Aires, Argentina
CLINIC_PHONE=+54-2324-123456
CLINIC_WEBSITE=https://clinicadental-mercedes.com.ar
BOT_PHONE_NUMBER=5491100000000

# ==================== PROFESSIONALS (FASE 2) ====================
# IDs de profesionales separados por comas (se crean en la base de datos)
# Ejemplo: 1 (Odontología General), 2 (Endodoncia), 3 (Ortodoncia)
PROFESSIONAL_IDS=1,2,3

# ==================== GOOGLE CALENDAR (OPTIONAL - FASE 2 EXTENDIDA) ====================
GOOGLE_CALENDAR_ID=clinic_email@clinica-dental.com
GOOGLE_OAUTH_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your-google-client-secret
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:8000/oauth/google/callback

# ==================== SMTP CONFIGURATION (HANDOFF) ====================
# Email para derivaciones humanas
HANDOFF_EMAIL=operadores@clinica-dental.com
HANDOFF_TARGET_EMAIL=operadores@clinica-dental.com

# SMTP Server (Gmail, Outlook, Custom Server)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-or-token
SMTP_SECURITY=SSL  # Options: SSL, STARTTLS, NONE

# ==================== WHATSAPP INTEGRATION ====================
YCLOUD_API_KEY=your-ycloud-api-key
YCLOUD_API_SECRET=your-ycloud-api-secret
YCLOUD_WEBHOOK_URL=https://your-domain.com/webhook/whatsapp

# ==================== CORS CONFIGURATION ====================
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000,https://your-domain.com

# ==================== ENVIRONMENT ====================
ENVIRONMENT=development  # Options: development, staging, production
LOG_LEVEL=info  # Options: debug, info, warning, error

# ==================== SERVICE PORTS (DOCKER) ====================
ORCHESTRATOR_PORT=8000
PLATFORM_UI_PORT=80
WHATSAPP_SERVICE_PORT=8002

# ==================== OPTIONAL: TIENDA NUBE (LEGACY) ====================
# Si se necesita mantener integración con Tienda Nube (opcional en FASE 2)
TIENDANUBE_STORE_ID=your-store-id
TIENDANUBE_ACCESS_TOKEN=your-access-token
GLOBAL_TN_STORE_ID=your-store-id
GLOBAL_TN_ACCESS_TOKEN=your-access-token
TIENDANUBE_USER_AGENT=Orchestrator-Dental/2.0

# ==================== OPTIONAL: APPOINTMENT SETTINGS ====================
# Duración estándar de turno (minutos)
DEFAULT_APPOINTMENT_DURATION=60

# Rango de disponibilidad (horas)
CLINIC_HOURS_START=09:00
CLINIC_HOURS_END=18:00
CLINIC_LUNCH_START=13:00
CLINIC_LUNCH_END=14:00

# Días de cierre
CLINIC_CLOSED_DAYS=0,6  # 0=Sunday, 1=Monday, ..., 6=Saturday

# ==================== OPTIONAL: NOTIFICATION SETTINGS ====================
# SMS para confirmación de turnos (si se implementa)
SMS_PROVIDER=twilio  # Options: twilio, vonage
SMS_API_KEY=your-sms-api-key
SMS_API_SECRET=your-sms-api-secret
SMS_FROM_NUMBER=+1234567890

# ==================== OPTIONAL: ANALYTICS & MONITORING ====================
# Sentry (Error Tracking)
SENTRY_DSN=https://your-sentry-key@sentry.io/project-id

# DataDog (Monitoring)
DATADOG_API_KEY=your-datadog-api-key
DATADOG_ENVIRONMENT=production

# ==================== DOCKER COMPOSE ====================
# Usuario/Password para servicios Docker (si los usa)
POSTGRES_USER=clinica_user
POSTGRES_PASSWORD=secure-password-here
POSTGRES_DB=clinica_dental_db

# ==================== DESARROLLO LOCAL ====================
DEBUG=False  # Cambiar a True para development
WORKERS=4    # Número de workers uvicorn

# ==================== NOTES ====================
# 1. NUNCA commitear .env a Git - usar .env.example como template
# 2. En Easypanel, copiar estos valores a las variables de entorno del servicio
# 3. Para producción, usar servicios de secretos (AWS Secrets Manager, Azure Key Vault, etc.)
# 4. Las credenciales SMTP y OpenAI son obligatorias
# 5. Las variables CLINIC_* son específicas para FASE 2 (Dental)
# 6. Google Calendar es opcional (para FASE 2 extendida)
# 7. Revisar AGENTS.md para descripción completa de cada variable
